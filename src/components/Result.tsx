const Result = ({ children }: { children: string }) => {
    return (
        <div className="text-center text-lg max-w-lg px-5 mx-auto text-white">
            {children}
        </div>
    );
};

export default Result;
